const refs = {
    footerInputEl: document.querySelector('.js-registration-input'),
    footerSubmitBtn: document.querySelector('.js-submit-btn')
}


console.dir(refs.footerInputEl)

const submitRegistrationForm = event => {
    event.
}


refs.footerSubmitBtn.addEventListener('submit', submitRegistrationForm)